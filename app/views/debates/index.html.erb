<h1>All debates</h1>

<ul class ="debates">
	<%= @debates.each do |debate| %>
		<li> <%= link_to "#{debate.topic} lasting #{debate.length} minutes", edit_debate_path(debate.id) %> 
		| <%= link_to "delete", debate, :method => :delete %> </li>
	<% end %>
<ul>